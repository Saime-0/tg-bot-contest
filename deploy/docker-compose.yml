services:
  tgbotcontest:
    env_file: .env
    restart: unless-stopped
    logging:
      driver: journald
    volumes:
      - ~/.local/share/tgbotcontest/main.db:/var/lib/tgbotcontest/main.db
    build:
      context: ../
      dockerfile: ./deploy/Dockerfile
    container_name: tgbotcontest